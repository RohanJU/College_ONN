// src/components/layout/MainLayout.js

import React from "react";

const MainLayout = ({ children }) => {
  return (
    <div>
      {/* Header, Nav, Footer, etc. can go here */}
      {children}
    </div>
  );
};

export default MainLayout;
